# @run: haxelib run-dir travix ${HAXESHIM_LIBCACHE}/travix/0.12.2/haxelib
# @post-install: haxe -cp src --run travix.PostDownload
# @install: lix --silent download "haxelib:travix#0.12.2" into travix/0.12.2/haxelib
-D travix=0.12.2
-cp ${HAXESHIM_LIBCACHE}/travix/0.12.2/haxelib/src

-lib tink_cli